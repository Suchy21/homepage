html {
    box-sizing: border-box;
}

*, ::after, ::before {
    box-sizing: inherit;
}
.body {
    max-width: 1000px;
    margin: auto;
}
.body--greyBackground {
    background-color: rgb(90, 90, 90);
    max-width: 1000px;
    margin: auto;
    color: #eee;
}
.header {
    color: rgb(0, 0, 0);
    padding: 7px;
}
.darkHeader {
    color: #eee;
    padding: 7px;
}

